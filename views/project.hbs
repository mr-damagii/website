<!DOCTYPE html>

<html lang="en">
    {{> head}}

    <body>
        <div class="container project">
            {{> nav}}

            {{> header this}}
            
            <article>
                {{{markdown body}}}
            </article>

            {{> footer}}
        </div>

        {{> scripts}}
    </body>
</html>