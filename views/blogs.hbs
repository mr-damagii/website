<!DOCTYPE html>

<html lang="en">
    {{> head}}

    <body>
        <div class="container">
            {{> header header}}

            {{> nav}}

            {{#if blogs}}
                <section class="blogs">
                    <ul>
                        {{#each blogs}}
                            <li>
                                <a href="/blog/{{url}}">
                                    <div class="image-container">
                                        {{{thumbnailPicture header.thumbnail header.largeThumbnail}}}
                                    </div>

                                    <div>
                                        <h3>{{header.title}}</h3>
                                    </div>

                                    <div>
                                        <p>{{header.summary}}</p>
                                    </div>
                                </a>
                            </li>
                        {{/each}}
                    </ul>
                </section>
            {{else}}
                {{> nothing}}
            {{/if}}

            {{> footer}}
        </div>

        {{> scripts}}
    </body>
</html>